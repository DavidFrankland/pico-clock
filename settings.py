import clock

# whether to display the "pico clock" startup banner
display_startup_animation = True

# the transition syle to use
transition_style = clock.TransitionStyle.blink1

# whether to cycle through all the transitions
cycle_transitions = False

# wheether to display the IP address
display_ip_address = True
